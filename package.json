{
  "name": "limbus-team-builder",
  "private": true,
  "packageManager": "pnpm@10.26.2",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "scrape": "pnpm -C packages/scraper scrape",
    "api": "pnpm -C apps/api dev",
    "web": "pnpm -C apps/web dev"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
